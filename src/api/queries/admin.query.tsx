import { useQuery } from '@tanstack/react-query'
import { api } from '../api'

// export const useGetMeQuery = () => {
//     const getMe = async () => await api.get('/admin/me')
//   const me = useQuery({ queryKey: ['todos'], queryFn: fetchTodoList })

//   return { me }
// }
